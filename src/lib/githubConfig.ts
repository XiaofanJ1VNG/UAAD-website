// Fill these in once you've created + published the GitHub repo, then
// commit this change. None of this is secret — it's just telling the site
// where to read/write its own content.
export const GITHUB_OWNER = "XiaofanJ1VNG";
export const GITHUB_REPO = "UAAD-website";
export const GITHUB_BRANCH = "main";

export const EVENTS_PATH = "content/events.json";
export const UPLOADS_DIR = "content/uploads";

export const RAW_BASE = `https://raw.githubusercontent.com/${GITHUB_OWNER}/${GITHUB_REPO}/${GITHUB_BRANCH}`;
export const API_BASE = `https://api.github.com/repos/${GITHUB_OWNER}/${GITHUB_REPO}`;
